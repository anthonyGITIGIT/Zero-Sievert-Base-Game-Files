function scr_numera_player_stash(arg0, arg1, arg2)
{
    var xx = arg0;
    var yy = arg1;
    var num = arg2;
    var toll = 2;
    
    if (x >= (xx - toll) && x <= (xx + toll) && y >= (yy - toll) && y <= (yy + toll))
        my_id = num;
}
